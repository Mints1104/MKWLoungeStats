# Environment Variables

## Backend (.env in root)
# Optional: CORS origin for production
FRONTEND_URL=https://your-app.vercel.app

## Frontend (.env in frontend/)
# API base URL - leave empty for production (defaults to /api)
# VITE_API_BASE=http://localhost:3000/api  # For local development only
